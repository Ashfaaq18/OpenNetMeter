<UserControl x:Class="WhereIsMyData.Views.DataUsageSummaryV"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:WhereIsMyData.ViewModels"
             mc:Ignorable="d" 
             UseLayoutRounding="True" 
             d:DesignHeight="370" d:DesignWidth="650">
    <!-- UseLayoutRounding="True" <fixes blurry text/> -->
    <UserControl.Resources>
        <vm:UnitConverter x:Key="converter"/>
    </UserControl.Resources>
    <Grid>
        <Grid Background="#f1f1f1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <Border Margin="25 25 12.5 25" Background="White" CornerRadius="10">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="20" Color="#dddddd" Opacity="0.4" Direction="280" ShadowDepth="0" />
                    </Border.Effect>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.25*"/>
                            <ColumnDefinition Width="0.75*"/>
                            <ColumnDefinition Width="1.25*"/>
                            <ColumnDefinition Width="0.75*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="2*"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.ColumnSpan="4"  Text="Current session data usage" VerticalAlignment="Center" Foreground="#717171" Margin="10 0 10 0" />
                        <Label Grid.Row="1" FontSize="15" x:Name="downloadData" HorizontalAlignment="Right" VerticalAlignment="Center" Content="{Binding CurrentSessionDownloadData.DataValue }"></Label>
                        <Label Grid.Column="1" Grid.Row="1" FontSize="15" x:Name="downloadDataSuffix" HorizontalAlignment="Left" VerticalAlignment="Center" Content="{Binding CurrentSessionDownloadData.DataSuffix, Converter={StaticResource converter}}" ></Label>
                        <Label Grid.Column="2" Grid.Row="1" FontSize="15" x:Name="uploadData" HorizontalAlignment="Right" VerticalAlignment="Center" Content="{Binding CurrentSessionUploadData.DataValue }"></Label>
                        <Label Grid.Column="3" Grid.Row="1" FontSize="15" x:Name="uploadDataSuffix" HorizontalAlignment="Left" VerticalAlignment="Center" Content="{Binding CurrentSessionUploadData.DataSuffix, Converter={StaticResource converter}}" ></Label>
                    </Grid>

                </Border>

                <Border Grid.Column="2" Margin="12.5 25 25 25" Background="White" CornerRadius="10">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="20" Color="#dddddd" Opacity="0.4" Direction="280" ShadowDepth="0" />
                    </Border.Effect>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="2*"/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.ColumnSpan="4" Text="Total data usage from : " VerticalAlignment="Center" Foreground="#717171" Margin="10 0 10 0"/>
                        <Label Grid.Row="1" FontSize="15" x:Name="TotaldownloadData" HorizontalAlignment="Right" VerticalAlignment="Center" Content="{Binding TotalDownloadData.DataValue }"></Label>
                        <Label Grid.Column="1" Grid.Row="1" FontSize="15" x:Name="TotaldownloadDataSuffix" HorizontalAlignment="Left" VerticalAlignment="Center" Content="{Binding TotalDownloadData.DataSuffix, Converter={StaticResource converter}}" ></Label>
                        <Label Grid.Column="2" Grid.Row="1" FontSize="15" x:Name="TotaluploadData" HorizontalAlignment="Right" VerticalAlignment="Center" Content="{Binding TotalUploadData.DataValue }"></Label>
                        <Label Grid.Column="3" Grid.Row="1" FontSize="15" x:Name="TotaluploadDataSuffix" HorizontalAlignment="Left" VerticalAlignment="Center" Content="{Binding TotalUploadData.DataSuffix, Converter={StaticResource converter}}" ></Label>
                    </Grid>

                </Border>

            </Grid>
            <Border Grid.Row="1" Margin="25 0 25 15" Background="White" CornerRadius="10">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" Color="#dddddd" Opacity="0.4" Direction="280" ShadowDepth="0" />
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="25"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="250"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="75"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="75"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="1" Text="Download Speed : " FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        <WrapPanel Grid.Column="2" VerticalAlignment="Center" >
                            <Label FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Left" Content="{Binding DownloadSpeed.DataValue}"/>
                            <Label FontSize="11" Margin="-5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" Content="{Binding DownloadSpeed.DataSuffix, Converter={StaticResource converter}}"/>
                            <TextBlock Text="ps" Margin="-5,0,0,0" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Left" />
                        </WrapPanel>
                        
                        <TextBlock Grid.Column="3" Text="Upload Speed : " FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                        <WrapPanel Grid.Column="4" VerticalAlignment="Center">
                            <Label FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Left" Content="{Binding UploadSpeed.DataValue}"/>
                            <Label FontSize="11" Margin="-5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left" Content="{Binding UploadSpeed.DataSuffix, Converter={StaticResource converter}}"/>
                            <TextBlock Margin="-5,0,0,0" Text="ps" FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Left" />
                        </WrapPanel>
                    </Grid>
                    <Border Grid.Row="1" Margin="25 0 25 25"  BorderThickness="1,0,0,1" BorderBrush="Gray"  >
                        <Polyline Name="_myPolyline" Stroke="Blue" StrokeThickness="2" Points="{Binding LtPoint}" ></Polyline>
                    </Border>
                </Grid>
            </Border>
        </Grid>

    </Grid>
</UserControl>
